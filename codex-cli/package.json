{
  "name": "@openai/codex",
  "version": "0.0.0-dev",
  "license": "Apache-2.0",
  "bin": {
    "codex": "bin/codex.js"
  },
  "type": "module",
  "scripts": {
    "build": "node ./scripts/dev_build.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "bin",
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/openai/codex.git"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --splitting --dts",
    "build:watch": "tsup src/index.ts --format esm,cjs --splitting --dts --watch",
    "test": "vitest run",
    "lint": "prettier --check 'src/**/*.{ts,tsx}' 'tests/**/*.ts'"
  },
  "dependencies": {
    "@vscode/ripgrep": "^1.15.14"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "prettier": "^3.3.3",
    "ts-node": "^10.9.2",
    "tsup": "^8.3.0",
    "typescript": "^5.6.2",
    "vitest": "^1.6.0"
  }
}
